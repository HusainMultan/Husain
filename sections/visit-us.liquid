{% render 'visit-us-inline', eyebrow: section.settings.eyebrow, heading: section.settings.heading %}

{% schema %}
{
  "name": "Visit us",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Our Showrooms" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Visit Karun Jewellers" }
  ],
  "presets": [{ "name": "Visit us" }]
}
{% endschema %}
